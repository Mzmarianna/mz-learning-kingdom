steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/your-image:$SHORT_SHA', '.']
    # If your Dockerfile is in a subfolder (e.g., 'server' or 'app'), uncomment and set:
    # dir: 'server'
images:
  - 'gcr.io/$PROJECT_ID/your-image:$SHORT_SHA'