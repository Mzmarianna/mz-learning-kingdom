rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if request.auth != null;
      // Using custom claims to match Firestore rules pattern
      allow write: if request.auth != null
        && request.auth.token.role == 'admin';
    }
  }
}